# Connect IQ build output
/bin/

# Connect IQ signing key — PRIVATE. Keep this secret; never commit it.
developer_key
*.der
*.pem

# Local editor / assistant settings
.claude/settings.local.json

# Sprite pre-processing backups (not needed to build)
/resources/sprites/_orig/

# Source spritesheet the faces were extracted from (not needed to build)
/assets/doom-faces.png

# OS cruft
.DS_Store
Thumbs.db
